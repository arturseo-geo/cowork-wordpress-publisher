---
description: Run a full WordPress site health check — plugins, PHP errors, cache, SSL, broken links, response time, and backup status.
---

Use the site-health skill to run a comprehensive health check.

Arguments: $ARGUMENTS

If a domain or site URL is provided, use it as the base for all checks.
If no argument, check the WordPress site connected via the WordPress MCP connector.

Return the full health report with critical issues first.
